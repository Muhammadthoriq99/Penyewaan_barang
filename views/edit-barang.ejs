<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Barang</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>Edit Barang</h1>
    </header>
    
    <form method="POST" action="/edit-barang/<%= barang._id %>">
        <label for="nama_barang">Nama Barang:</label>
        <input type="text" id="nama_barang" name="nama_barang" value="<%= barang.nama_barang %>" required>
        <br>

        <label for="kategori">Kategori:</label>
        <input type="text" id="kategori" name="kategori" value="<%= barang.kategori %>" required>
        <br>

        <label for="jumlah">Jumlah:</label>
        <input type="number" id="jumlah" name="jumlah" value="<%= barang.jumlah %>" required>
        <br>

        <label for="kondisi">Kondisi:</label>
        <input type="text" id="kondisi" name="kondisi" value="<%= barang.kondisi %>" required>
        <br>

        <label for="deskripsi">Deskripsi:</label>
        <textarea id="deskripsi" name="deskripsi" required><%= barang.deskripsi %></textarea>
        <br>

        <button type="submit">Simpan Perubahan</button>
    </form>

    
    <br>
    <a href="/">Kembali ke Daftar Peminjaman</a>
</body>
</html>